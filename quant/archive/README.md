# archive/ —— 归档区 (不参与任何运行, 只为可追溯)

这里的东西**都不要再跑**。留着只是为了将来有人问"当初那个数是怎么来的"时能对上。

## legacy/
| 文件 | 说明 |
|---|---|
| `rolling10y_dual_model.py` | 重构前的 760 行单体脚本, 产出过 24.88% 那版结果。逻辑已 1:1 拆进 `core/` + `run_rolling.py`, 二者曾做过零偏差复现对照。 |
| `fix_qlib_seam.py` / `fix_seam.log` | 给 `cn_data` 打"行情断点补丁"的脚本。**方案已废弃** —— 它补的那 358 只恰好是用今天财务数据算出来的池子, 补丁本身就是前视偏差的来源。现在改为用 `tools/fetch_ohlcv.py` 整体重抓全市场。 |

## stale_results_20260731/
2026-07-31 之前产出的 `rolling10y_*.csv/log`。**这些数字全部作废**, 原因:
1. 可交易宇宙只有 358 只有近年行情, 且是按未来信息挑出来的 (选择偏差 + 存活偏差);
2. 流动性口径用了复权价而非真实价;
3. `sh600256` 存在 14 个负收盘价 (前复权逐次减息导致), 污染标签与特征。

修完以上三条后必须在新数据上重跑, 不得引用本目录任何数字。

## tmp_scripts/
排查期间的一次性脚本 (`_tmp_*.py`), 依赖当时的临时输出路径, 大概率已跑不通。
