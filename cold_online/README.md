# cold_online —— Paper Trading 策略冷启动观察区

**目录定位**：所有已进入 Paper Trading（虚拟盘）验证的策略档案。每个策略一个子目录、一份《上线报告.md》，记录从离线策略定义到上线判定的全部证据链。**目标：积累多个策略的纸面验证实例后，从中总结出可复用的"上线判定规则"——满足什么条件，一个策略可以从这里毕业进入实盘。**

**建立日期**: 2026-09-20

---

## 一、当前在跑策略（3 个）

| 策略 | 类型 | 回测年化(剔20)¹ | Sharpe | β | 回测回撤 | PT 至今² | 下次换仓 |
|---|---|---:|---:|---:|---:|---:|---|
| **ICW_SW** ICW双周+熊市切VGH | 含模型·进攻 | 18.5~19.3% | 1.35 | 0.54 | -20.9% | **-2.78%** | 09-23（双周） |
| **VG** value+盈利 Top10 | 纯规则·稳健 | 12.1~15.9% | 1.10 | 0.40 | -17.5% | **-2.63%** | 09-30（月末） |
| **VGH** 结构化剥离 Top10 | 纯规则·防御 | 11.8~15.3% | 1.23 | 0.29 | -17.8% | **-3.23%** | 09-30（月末） |

¹ 存在两份基线口径（详见各报告第二节），区间两端分别来自《V18策略上线报告.md》与 `outputs/rolling_ablate_sumd.log`。  
² 截至 2026-09-18 收盘，建仓 7 个交易日，样本无统计意义。沪深300 同期约 -1.5%。

三策略共同点：同一"十年双正"股票池（382 只含 55 港股）、选股 alpha 主导（占总收益 94~103%）、价值风格。

**注意**：ICW_SW 熊市腿（70%）与 VGH 同源，两者持仓高度重叠，不是分散组合。

---

## 二、目录结构

```
cold_online/
├── README.md            # 本文件：总览 + 上线判定规则（持续修订）
├── ICW_SW/上线报告.md    # 含模型进攻策略
├── VG/上线报告.md        # 纯规则稳健策略
└── VGH/上线报告.md       # 纯规则防御策略
```

每个策略报告固定章节：策略是什么 → 上线前冻结收益（回测基线）→ 收益来源（归因）→ 上线买入股票（持仓）→ Paper Trading 表现 → 预计收益 → 已知风险与简化假设 → 上线判定适用性。

新策略进入 Paper Trading 时，复制此结构建子目录。

---

## 三、运行链路（数据到台账）

| 环节 | 命令/文件 | 频率 |
|---|---|---|
| 每日盯市 | `bash live/daily_mark.sh`（~15 秒）或刷新看板自动同步 | 每交易日 |
| Web 看板 | `streamlit run live/dashboard.py`（localhost:8501） | 常驻 |
| 换仓（ICW_SW 双周） | `bash live/rebalance.sh 2026-09-23 ICW_SW` | 每 10 交易日 |
| 换仓（VG/VGH 月末） | `bash live/rebalance.sh 2026-09-30 ALL` | 每月末 |

链路顺序（换仓）：A 股行情更新 → 重建 bin + 港股 quotes 刷新 + 合并 → 估值缓存重建 → 信号生成 → T+1 模拟成交 + 盯市。

台账：`qlib/quant/live/ledger/{策略}/`（state.json + trades.csv）  
信号：`qlib/quant/live/signals/{日期}/{策略}/`

---

## 四、上线判定规则（草案 v0.1 · 随实例持续修订）

> 本节是 cold_online 的核心产出：**从纸面验证中提炼"什么条件满足才上实盘"**。当前规则综合自 V18 上线方案（2026-09-10）与首期 Paper Trading 暴露的实际问题。随策略实例积累逐条修订，修订需注明日期与依据。

### 4.1 硬性前置条件（Gate，一票否决）

| # | 条件 | 现状 |
|---|---|---|
| G1 | **数据链路稳定**：Paper Trading 期间无未修复的数据断供（行情/估值/基本面） | ⚠️ 港股曾断供 2 个月（07-23~09-16），已修复，观察期重新起算 |
| G2 | **回测基线口径唯一**：分年收益、年化、交易次数在所有文档中一致 | ❌ 当前双口径冲突（剔20 年化差 1.7~3.5pp），须统一重跑 |
| G3 | **执行可行性**：模拟的每手股数、最小下单金额与真实市场规则一致 | ❌ 港股按 100 股/手模拟，实盘前必须改为真实手数表 |
| G4 | **信号可复现**：同一信号日重跑（同代码同数据）订单一致，模型 SHA256 校验通过 | ✅ 已具备（冻结模型 + config hash 校验） |

### 4.2 统计验证条件（观察期 ≥ 3 个月）

| # | 条件 | 判定口径 |
|---|---|---|
| S1 | 实盘 Sharpe ≥ 回测 Sharpe × 0.8 | 滚动 3 个月日收益计算 |
| S2 | 最大回撤 ≤ 回测最大回撤 × 1.2 | 建仓起累计 |
| S3 | 实盘与回测同期年化偏差 ≤ 5pp | 以回测同期逐日净值对照 |
| S4 | 滑点偏差 ≤ 0.5pp/年 | 模拟成交价 vs 实际可成交价对比（换手越高越关键） |

### 4.3 分层判定（不同策略不同门槛）

| 层 | 适用 | 额外条件 | 仓位定位 |
|---|---|---|---|
| L1 纯规则稳健 | VG | S1~S4 达标即上线 | 底仓（资金占比高） |
| L2 纯规则防御 | VGH | S1~S4 + 港股执行可行性（G3） | 底仓 |
| L3 含模型进攻 | ICW_SW | S1~S4 + **模型漂移监控**（月度 IC 衰减 <30%）+ 双倍观察期（6 个月） | 增强仓（初始 ≤30%） |

### 4.4 止损与降级规则（上线后持续）

| 触发 | 动作 |
|---|---|
| 实盘 Sharpe < 回测 × 0.5 | 暂停加仓 |
| 回撤 > 回测 × 1.5 | 减仓 50% |
| 连续 3 月年化偏差 > 5pp | 停止该策略 |

### 4.5 资金爬坡（上线后）

第 1 个月 10% 目标资金 → 第 2-3 个月 50% → 第 4-6 个月 100%。纯规则 70% / 含模型 30% 起步，L3 达标后可提至 50%。

---

## 五、已归档的关键事件

| 日期 | 事件 | 影响 |
|---|---|---|
| 2026-09-09 | 三策略信号冻结（git e1717b41）+ 模型 W2027 冻结 | 上线基线 |
| 2026-09-10 | 虚拟建仓（各 50,000 元，T+1 成交） | PT 起点 |
| 2026-07-23~09-16 | 港股行情断供（fetch_hk_data 缓存跳过 bug） | VGH/VGH腿/ICW模型腿全部退化，VG=VGH 订单一致 |
| 2026-09-17 | 断供修复 + 三策略按 9/9 信号重放建仓 | 当前持仓为修正后口径；观察期从 09-17 重新起算 |
| 2026-09-20 | cold_online 目录建立，三策略上线报告归档 | 本文档 |

---

## 六、上线判定规则总结模板（未来从本目录产出）

当积累 ≥3 个策略、各 ≥3 个月 PT 记录后，从本目录所有报告反推提炼：

1. **共性通过条件**（所有活下来的策略共同满足的）
2. **共性失败信号**（所有死掉的策略共同暴露的）
3. **分层门槛校准**（纯规则 vs 含模型的最小观察期、Sharpe 衰减容忍度）
4. 最终形成一份《实盘上线判定标准 v1.0》，替代本草案

---

**相关文档**：根目录《V18策略上线报告.md》（原始上线方案）· `qlib/quant/docs/策略说明书-小白版.md`（策略细节）· `qlib/quant/docs/策略演进史-V4到V19.md`
